#include <iostream>
#include <cmath>

using namespace std;

int main() {
	for(int line = 0; line < 7; line++) {
		for(int preSpace = 0; preSpace < abs((line/2) - line); preSpace++) {
			cout << " ";
		}
		cout << "#";
		for(int innerSpace = 0; innerSpace
	}

	return 0;
}
